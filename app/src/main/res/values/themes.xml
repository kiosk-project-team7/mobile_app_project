<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KioskUI" parent="Theme.AppCompat">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorBackgroundFloating">@color/white</item>
        <item name="android:colorBackgroundFloating" tools:targetApi="23">@color/white</item>
        <item name="android:textColor">#000000</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">#B4A6AB</item>
        <item name="colorPrimaryVariant">#946E83</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.KioskUI.Alert" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/teal_200</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColor">#000000</item>
        <!-- Used for the background -->
        <item name="android:background">@color/white</item>
    </style>

    <style name="Theme.KioskUI.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.KioskUI.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.KioskUI.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>